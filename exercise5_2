var input1 = 370;
var sum = 0;
var num = input1;
while (num > 0) {
  var digit = num % 10;
  sum += Math.pow(digit, 3);
  num = Math.floor(num / 10);
}
if (sum == input1) {
  console.log(true);
} else {
  console.log(false);
}